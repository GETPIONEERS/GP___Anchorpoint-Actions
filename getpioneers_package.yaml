# Anchorpoint Markup Language
# Predefined Variables: e.g. ${path}
# Environment Variables: e.g. ${MY_VARIABLE}
# Full documentation: https://docs.anchorpoint.app/Actions/Reference

version: 1.0
action:
  name: GETPIONEERS Actions

  id: gp::actions::package
  type: package
  enable: true
  author: GETPIONEERS GmbH
  description: GETPIONEERS Anchorpoint Actions
  details: 'GETPIONEERS Anchorpoint Actions'
  icon:
    path: Package_Icon.svg

  actions:
    - gp::actions::createfoldertemplate

  platforms:
    - win 