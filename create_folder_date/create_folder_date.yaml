# Anchorpoint Markup Language
# Predefined Variables: e.g. ${path}
# Environment Variables: e.g. ${MY_VARIABLE}
# Full documentation: https://docs.anchorpoint.app/Actions/Reference

version: 1.0
action:
  name: Create Folder (Current Date)

  version: 1
  id: gp::actions::createfolderdate
  type: python
  author: René Frassek (GETPIONEERS GmbH)
  description: Creates folder with the current date as name (YYYY-MM-DD)
  category: "GETPIONEERS/Folders"
  icon:
    path: /icons/GP_Icon.svg

  script: create_folder_date.py

  register:
    folder:
      enable: true